CREATE TABLE PlaceEntity (
    id TEXT NOT NULL ,
    name TEXT NOT NULL,
    formattedAddress TEXT NOT NULL,
    lat REAL NOT NULL,
    lng REAL NOT NULL,
    googleMapsUri TEXT NOT NULL,
    photo TEXT,
    photoUri TEXT,
    trip_id INTEGER,
    FOREIGN KEY(trip_id) REFERENCES TripEntity(id),
    PRIMARY KEY(id, trip_id)
);

getById:
SELECT * FROM PlaceEntity WHERE id = :placeId AND trip_id = :tripId;

getByTripId:
SELECT * FROM PlaceEntity WHERE trip_id = ?;

deleteById:
DELETE FROM PlaceEntity WHERE id = :placeId AND trip_id = :tripId;

deleteByTripId:
DELETE FROM PlaceEntity WHERE trip_id = ?;

insertOrReplace:
INSERT OR REPLACE INTO PlaceEntity VALUES ?;